#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

#define ABS(x) ((x) < 0 ? -(x) : (x))

/**
 * ABS - Computes the absolute value of a number
 * Description - A function-like macro that computes the value of a number x
 * @y: Provided number
 * Return: Nothing
 */

#endif
