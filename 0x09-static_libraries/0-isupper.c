#include "main.h"

/**
 * _isupper - Checks for upper case letters
 * @c: Returns 1
 * Description - Returns 0 for every other letter
 * Return: 1 for c and 0 for everyother letter
 */
int _isupper(int c)
{
	f (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
