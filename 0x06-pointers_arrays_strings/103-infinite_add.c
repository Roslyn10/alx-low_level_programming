#include "main.h"
/**
 * *infinite_add - Adds two integers
 * Description - Adds two gicen integers
 * @n1: First number
 * @n2: Second number
 * @r: The buffer that the function will use to stroe the result
 * size_r - the buffer size
 * Return: 0 Always (Success)
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	int len  = 0;
	int f = 0;
	char t;

	while (*(n +len) != '\0')
	{
		len++;
	}
	len--;


