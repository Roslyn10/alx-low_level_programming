#include "main.h"
/**
 * add: Adds two given numbers
 * Description: Adds two integers
 * Return: Result of Both integers
 * @s: Integer 1
 * @k: Integer 2
 */
int add(int s, int k)
{
	int result = s + k;

	return (result);
}
